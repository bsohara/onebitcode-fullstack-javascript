const doubleSpeed = (speed) => speed * 2 + "km/s.";

let spd = doubleSpeed(77);
console.log(spd);

const verifyAge = age => age >= 18 ? true : false;

let anos = verifyAge(12);

console.log(anos);

anos = verifyAge(23);

console.log(anos);