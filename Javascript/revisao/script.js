frontFrame = ['ReactJS', 'VueJS', 'NextJS'];
backFrame = ['NodeJS', 'BackboneJS', 'Express'];
mobiFrame = ['Ionic', 'React Native', 'Meteor'];
deskFrame = ['Electron', 'Meteor Desk', 'NodeGUI'];

var option = parseInt(prompt(`Qual stack utilizar?
                            \n1 - frontend
                            \n2 - backend
                            \n3 - mobile
                            \n4 - desktop`));

switch (option) {
  case 1:
    break;

  case 2:
    break;

  case 3:
    break;

  case 4:
    break;

  default:
}

